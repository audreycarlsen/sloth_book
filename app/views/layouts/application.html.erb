<!DOCTYPE html>
<html>
  <head>
    <title>SlothBook | Make Friends, Slowly</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>

<body>

  <div class='id_bar'>
    <% if current_user %>
      <%= link_to 'Home', newsfeed_path %> | <%= link_to "#{current_user.name}", user_path(current_user) %> | <%= link_to 'Edit', edit_user_path(current_user) %> | <%= link_to 'Log out', log_out_path %>
    <% else %>
      <%= link_to 'Log in', log_in_path %>
    <% end %>
  </div>

  <div class='header'>
    <h1>Welcome to Slothbook.</h1>
  </div>

  <div class='container'>
    <% if flash.notice %>
      <%= flash.notice %><br><br>
    <% end%>
    
    <%= yield %>
  </div>

</body>
</html>
