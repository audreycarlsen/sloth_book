<!DOCTYPE html>
<html>
<head>
  <title>SlothBook | Make Friends, Slowly</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- Put this stuff somewhere else. -->

<% if current_user %>
  <p>Logged in as <%= "#{current_user.first_name} #{current_user.last_name}" %>.</p>
  <p><%= link_to 'Log out', log_out_path %></p>
  <p><%= link_to 'Newsfeed', root_path %></p>
  <p><%= link_to 'My Wall', user_path(current_user) %></p>
  <p><%= link_to 'Edit my account', edit_user_path(current_user) %></p>
<% end %>

<h1>WELCOME TO SLOTHBOOK!!!</h1>

<%= yield %>

</body>
</html>
